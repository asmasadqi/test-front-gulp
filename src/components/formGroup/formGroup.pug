//- Import the checkbox component
include ../checkbox/checkbox
//- Import the button component
include ../button/button

mixin formGroup(beforeText, linkText, linkUrl, afterText, error)
  div.nj-form-group
    div.nj-form-group__control
      +checkbox('accept-terms', false)
      label(for="accept-terms").label-text
        | #{beforeText} 
        +button(linkText, 'nj-button--ghost', linkUrl, false)
        | #{afterText}
    div.nj-form-group__feedback-msg
      if error
        p.nj-form-group__error #{error}